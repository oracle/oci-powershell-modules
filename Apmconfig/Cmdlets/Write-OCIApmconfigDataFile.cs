/*
 * NOTE: Generated using OracleSDKGenerator, API Version: 20210201
 * DO NOT EDIT this file manually.
 *
 * Copyright (c) 2020, 2026, Oracle and/or its affiliates.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

using System;
using System.Management.Automation;
using Oci.ApmconfigService.Requests;
using Oci.ApmconfigService.Responses;
using Oci.ApmconfigService.Models;
using Oci.Common.Model;

namespace Oci.ApmconfigService.Cmdlets
{
    [Cmdlet("Write", "OCIApmconfigDataFile")]
    [OutputType(new System.Type[] { typeof(void), typeof(Oci.ApmconfigService.Responses.PutDataFileResponse) })]
    public class WriteOCIApmconfigDataFile : OCIConfigCmdlet
    {
        [Parameter(Mandatory = true, ValueFromPipelineByPropertyName = true, HelpMessage = @"The data file to be uploaded.", ParameterSetName = FromStreamSet)]
        public System.IO.Stream PutDataFileBody { get; set; }

        [Parameter(Mandatory = true, ValueFromPipelineByPropertyName = true, HelpMessage = @"Use this parameter to provide the file location from where the input stream to be read. The data file to be uploaded.", ParameterSetName = FromFileSet)]
        public String PutDataFileBodyFromFile { get; set; }

        [Parameter(Mandatory = true, ValueFromPipelineByPropertyName = true, HelpMessage = @"The name of the data file.")]
        public string DataFileName { get; set; }

        [Parameter(Mandatory = true, ValueFromPipelineByPropertyName = true, HelpMessage = @"The APM Domain ID the request is intended for.")]
        public string ApmDomainId { get; set; }

        [Parameter(Mandatory = true, ValueFromPipelineByPropertyName = true, HelpMessage = @"The type of the data file.")]
        public string ApmType { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"Optional base64-encoded MD5 hash of the request body. If provided, the server will perform a data integrity check by computing the MD5 of the received content and comparing it to the supplied value.

If the values do not match, the request will be rejected with an HTTP 400 error and a message such as:

""The computed MD5 of the request body (ACTUAL_MD5) does not match the Content-MD5 header (HEADER_MD5)""")]
        public string ContentMD5 { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"Optional parameter specifying the media type (MIME type) of the request or response body. If not specified, the default is `application/octet-stream`.

This value can be used by recipients to determine how to interpret or render the content.")]
        public string ContentType { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"Optional parameter that indicates the natural language of the content. This value can be used by clients or intermediaries to select or display content based on language preferences.")]
        public string ContentLanguage { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"Optional parameter indicating the content encodings applied to the request body (e.g., gzip, deflate). This value can be used by recipients to determine how to decode the content.")]
        public string ContentEncoding { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"Optional parameter that provides presentation information for how the content should be displayed or handled by the recipient.

For example, to prompt a file download with a custom filename: `attachment; filename=""example.txt""`")]
        public string ContentDisposition { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"A string containing a JSON-encoded object with metadata related to the uploaded file or resource. Example:   {""fileName"":""report.pdf"",""uploader"":""jane.doe"",""category"":""financial""}")]
        public string Metadata { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"A token that uniquely identifies a request so it can be retried in case of a timeout or server error without risk of executing that same action again. Retry tokens expire after 24 hours, but can be invalidated before then due to conflicting operations. For example, if a resource has been deleted and purged from the system, then a retry of the original creation request might be rejected.")]
        public string OpcRetryToken { get; set; }

        [Parameter(Mandatory = false, ValueFromPipelineByPropertyName = true, HelpMessage = @"The client request ID for tracing.")]
        public string OpcClientRequestId { get; set; }

        protected override void ProcessRecord()
        {
            base.ProcessRecord();
            PutDataFileRequest request;

            if (ParameterSetName.Equals(FromFileSet))
            {
                PutDataFileBody = System.IO.File.OpenRead(GetAbsoluteFilePath(PutDataFileBodyFromFile));
            }
            

            try
            {
                request = new PutDataFileRequest
                {
                    PutDataFileBody = PutDataFileBody,
                    DataFileName = DataFileName,
                    ApmDomainId = ApmDomainId,
                    ApmType = ApmType,
                    ContentMD5 = ContentMD5,
                    ContentType = ContentType,
                    ContentLanguage = ContentLanguage,
                    ContentEncoding = ContentEncoding,
                    ContentDisposition = ContentDisposition,
                    Metadata = Metadata,
                    OpcRetryToken = OpcRetryToken,
                    OpcClientRequestId = OpcClientRequestId
                };

                response = client.PutDataFile(request).GetAwaiter().GetResult();
                WriteOutput(response);
                FinishProcessing(response);
            }
            catch (OciException ex)
            {
                TerminatingErrorDuringExecution(ex);
            }
            catch (Exception ex)
            {
                TerminatingErrorDuringExecution(ex);
            }
        }

        protected override void StopProcessing()
        {
            base.StopProcessing();
            TerminatingErrorDuringExecution(new OperationCanceledException("Cmdlet execution interrupted"));
        }

        private PutDataFileResponse response;
        private const string FromFileSet = "FromFile";
        private const string FromStreamSet = "FromStream";
    }
}
